{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register'}}) }}
    {{ form_widget(form) }}
    <div>
        <button class="btn btn-lg btn-success" type="submit" value="{{ 'registration.submit'|trans }}"> Submit </button>
        <a class="btn btn-lg btn-info" href="/" role="button">Go back</a>
    </div>
{{ form_end(form) }}
